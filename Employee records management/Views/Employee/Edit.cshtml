@model EmployeeViewModel
@{
    ViewData["Title"] = "Edit Employee";
}

<section class="page-hero">
    <div>
        <span class="eyebrow">Profile Update</span>
        <h2>Refine employee details.</h2>
        <p>Update contact information, status, and department assignment while keeping the form readable and focused.</p>
    </div>
    <div class="hero-metric">
        <div>
            <span class="metric-number">#@Model.EmployeeId</span>
            <span class="metric-label">employee record</span>
        </div>
        <p class="mb-0">@Model.FirstName @Model.LastName</p>
    </div>
</section>

<section class="panel form-panel">
    <div class="panel-header">
        <div>
            <span class="eyebrow">Edit Form</span>
            <h3 class="panel-title">Employee details</h3>
        </div>
        <a asp-action="Index" class="btn btn-outline-dark">
            <i class="bi bi-arrow-left"></i>
            Back to employees
        </a>
    </div>

    <form asp-action="Edit" asp-controller="Employee" method="post" class="form-grid">
        <input type="hidden" asp-for="EmployeeId" />

        <div>
            <label asp-for="FirstName" class="form-label">First Name</label>
            <input asp-for="FirstName" type="text" class="form-control" />
            <span asp-validation-for="FirstName" class="text-danger"></span>
        </div>

        <div>
            <label asp-for="LastName" class="form-label">Last Name</label>
            <input asp-for="LastName" type="text" class="form-control" />
            <span asp-validation-for="LastName" class="text-danger"></span>
        </div>

        <div>
            <label asp-for="DateOfBirth" class="form-label">Date of Birth</label>
            <input asp-for="DateOfBirth" type="date" class="form-control" />
            <span asp-validation-for="DateOfBirth" class="text-danger"></span>
        </div>

        <div>
            <label class="form-label">Gender</label>
            <div class="choice-row">
                <label class="choice-box">
                    <input asp-for="Gender" class="form-check-input" value="male" type="radio" />
                    <span>Male</span>
                </label>
                <label class="choice-box">
                    <input asp-for="Gender" class="form-check-input" value="female" type="radio" />
                    <span>Female</span>
                </label>
            </div>
            <span asp-validation-for="Gender" class="text-danger"></span>
        </div>

        <div>
            <label asp-for="Email" class="form-label">Email</label>
            <input asp-for="Email" type="email" class="form-control" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>

        <div>
            <label asp-for="PhoneNumber" class="form-label">Phone Number</label>
            <input asp-for="PhoneNumber" type="tel" class="form-control" />
            <span asp-validation-for="PhoneNumber" class="text-danger"></span>
        </div>

        <div class="form-span-2">
            <label asp-for="Address" class="form-label">Address</label>
            <textarea asp-for="Address" class="form-control" rows="3"></textarea>
            <span asp-validation-for="Address" class="text-danger"></span>
        </div>

        <div>
            <label asp-for="DepartmentId" class="form-label">Department</label>
            <select asp-for="DepartmentId" asp-items="ViewBag.departments" class="form-select">
                <option value="">Select Department</option>
            </select>
            <span asp-validation-for="DepartmentId" class="text-danger"></span>
        </div>

        <div>
            <label class="form-label">Status</label>
            <label class="choice-box">
                <input asp-for="IsActive" class="form-check-input" type="checkbox" />
                <span>Mark as Active</span>
            </label>
            <span asp-validation-for="IsActive" class="text-danger"></span>
        </div>

        <div class="form-span-2 section-actions">
            <button type="submit" class="btn btn-primary">
                <i class="bi bi-check2"></i>
                Save Changes
            </button>
            <a asp-action="Index" class="btn btn-outline-dark">Cancel</a>
        </div>
    </form>
</section>
